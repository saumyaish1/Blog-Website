<%- include( "partials/header") %>
    <h1>HOME</h1>
    <p>
        <%= homeContent %>
    </p>
    <% posts.forEach(function (post){  %>
        <h1>

            <%= post.title %>
        </h1>
        <p>
            <% var bodySubstring = post.content.substring(0,100); %>
                <%= bodySubstring + "..." %>
                    <a href="/posts/<%=post._id%>">Read more</a>
        </p>

        <% }) %>

            <%- include( "partials/footer") %>